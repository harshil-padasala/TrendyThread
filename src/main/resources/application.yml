server:
  port: 8079

# Logging configuration: root level + package-specific overrides for controllers
logging:
  level:
    root: INFO
    com.trendythread.app: DEBUG
  file:
    path: logs/trendy-thread.log
    name: trendy-thread.log
    max-size: 10MB
    max-history: 30

spring:

  application:
    name: TrendyThread

  datasource:
    url: jdbc:postgresql://aws-0-us-east-1.pooler.supabase.com:6543/postgres?user=postgres.mntswotqhnptsbfmsyep&password=8BLyQ-w*7Z9kLmD
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false